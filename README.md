# PR Reviewer Assignment Service

Тестовое задание для стажировки Backend (Авито, осенняя волна 2025)

Сервис реализует автоматическое назначение ревьюеров на Pull Request’ы, управление командами и пользователями, а также операции merge и переназначения ревьюеров — **полностью в соответствии с предоставленной спецификацией `openapi.yaml`**.

---

# 📌 Краткое описание

Цель — создать микросервис, который:

* управляет командами и пользователями;
* автоматически назначает **до двух** активных ревьюверов из команды автора PR;
* обеспечивает **идемпотентный merge**;
* позволяет **переназначать** ревьюверов согласно бизнес-правилам;
* предоставляет вспомогательные запросы: список PR, назначенных пользователю, и статистику назначений (доп. задание).

Сервис работает через HTTP API, полностью соответствует структурам запросов/ответов и коду ошибок, описанным в `openapi.yaml`.

---

# 🧩 Что реализовано

### ✔ Основные требования задания

| Требование                                                                     | Выполнено                      |
| ------------------------------------------------------------------------------ | ------------------------------ |
| Полное соответствие OpenAPI (эндпоинты, DTO, структура ошибок, поведение)      | **Да**                         |
| Создание команды + массовое создание/обновление пользователей                  | **Да**                         |
| Получение команды и списка участников                                          | **Да**                         |
| Управление активностью пользователей (`isActive`)                              | **Да**                         |
| Создание PR с выбором ревьюверов по правилам                                   | **Да**                         |
| Случайный выбор до 2 активных ревьюверов из команды автора                     | **Да**                         |
| Исключение автора из списка ревьюверов                                         | **Да**                         |
| Переназначение ревьювера на случайного активного участника команды заменяемого | **Да**                         |
| Идемпотентная операция merge                                                   | **Да**                         |
| Получение списка PR, где пользователь является ревьювером                      | **Да**                         |
| Поднятие сервиса через `docker-compose up` + миграции                          | **Да**                         |
| Сервис по умолчанию слушает порт 8080                                          | **Да**                         |
| RPS ≤ 5, SLI ≤ 300мс для основных операций                                     | **Да** (запросы <5мс локально) |

---

# ⭐ Дополнительные задания

| Дополнительное задание                                           | Выполнено                                          |
| ---------------------------------------------------------------- | -------------------------------------------------- |
| Эндпоинт статистики назначений (`/stats/assignments`)            | **Да**                                             |
| Нагрузочное тестирование                                         | **Да** (см. раздел ниже)                           |
| Массовая деактивация пользователей и каскадная переназначаемость | **Нет** (времени на реализацию не хватило)         |
| Интеграционные/E2E тесты                                         | **Частично** (покрыт usecase+repo, HTTP не покрыт) |
| Конфигурация линтера                                             | **Нет** (описано в TODO)                           |

---

# 🏗 Архитектура

Сервис построен по чистой многослойной архитектуре:

```
cmd/app/main.go — точка входа, DI
internal/http — HTTP handlers, DTO, ошибки
internal/usecase — бизнес-логика
internal/domain — доменные сущности и доменные ошибки
internal/repository — интерфейсы репозиториев
internal/repository/postgres — реализация поверх PostgreSQL (pgx)
internal/platform/db — конфиг БД, транзакции
internal/platform/log — структурированные логгеры (slog)
migrations/ — SQL-миграции (goose)
openapi.yaml — спецификация API Авито
```

## Ключевые особенности реализации

* **Usecase-слой не знает о PostgreSQL** — работает только с интерфейсами и `DBExecutor`.
* **Транзакции через TxManager**.
* **Идемпотентный merge** — повторный вызов возвращает существующий результат.
* **Полностью детерминированная бизнес-логика** выбора ревьюверов.
* **Доменные ошибки со строгими кодами** — соответствуют OpenAPI.

---

# 🧪 Нагрузочное тестирование

Тест производился через `hey`:

### `/pullRequest/create`, 1000 запросов, 20 concurrency

```
Requests/sec: 1282
Average latency: 1.7ms
All requests < 10ms
No errors
```

### `/pullRequest/reassign`

```
Requests/sec: 1150
Average latency: 2.1ms
No errors
```

### Вывод

Требуемые SLI (≤300мс) выдерживаются с огромным запасом.

---

# ⚙️ Запуск проекта

## 1. Создать `.env` в корне:

```env
HTTP_PORT=8080

DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_NAME=avito_fall_internship
DB_PASSWORD=password
POSTGRES_DSN=postgres://postgres:password@postgres:5432/avito_fall_internship?sslmode=disable

LOG_LEVEL=DEBUG
MIGRATIONS_DIR=./migrations/
REQUEST_TIMEOUT_MS=300
READ_HEADER_TIMEOUT_MS=100
```

## 2. Собрать и запустить:

```bash
docker compose build
docker compose up
```

Сервис станет доступен на:

```
http://localhost:8080
```

---

#  Примеры `curl` для всех эндпоинтов

### `POST /team/add`

Создать команду и участников (создаёт / обновляет юзеров):

```bash
curl -X POST "http://localhost:8080/team/add" \
  -H "Content-Type: application/json" \
  -d '{
        "team_name": "backend",
        "members": [
          { "user_id": "u1", "username": "Alice", "is_active": true },
          { "user_id": "u2", "username": "Bob",   "is_active": true },
          { "user_id": "u3", "username": "Carol", "is_active": false }
        ]
      }'
```

Ожидаемый ответ `201`:

```json
{
  "team": {
    "team_name": "backend",
    "members": [
      { "user_id": "u1", "username": "Alice", "is_active": true },
      { "user_id": "u2", "username": "Bob",   "is_active": true },
      { "user_id": "u3", "username": "Carol", "is_active": false }
    ]
  }
}
```

---

### `GET /team/get`

Получить команду с участниками:

```bash
curl "http://localhost:8080/team/get?team_name=backend"
```

Ответ `200`:

```json
{
  "team_name": "backend",
  "members": [
    { "user_id": "u1", "username": "Alice", "is_active": true },
    { "user_id": "u2", "username": "Bob",   "is_active": true },
    { "user_id": "u3", "username": "Carol", "is_active": false }
  ]
}
```

---

### `POST /users/setIsActive`

Деактивировать / активировать пользователя:

```bash
curl -X POST "http://localhost:8080/users/setIsActive" \
  -H "Content-Type: application/json" \
  -d '{
        "user_id": "u3",
        "is_active": true
      }'
```

Ответ `200`:

```json
{
  "user": {
    "user_id": "u3",
    "username": "Carol",
    "team_name": "backend",
    "is_active": true
  }
}
```

Если пользователь не найден — `404` с `ErrorResponse`.

---

### `GET /users/getReview`

Получить PR-ы, где пользователь назначен ревьюером:

```bash
curl "http://localhost:8080/users/getReview?user_id=u2"
```

Ответ `200` (пример):

```json
{
  "user_id": "u2",
  "pull_requests": [
    {
      "pull_request_id": "pr-1001",
      "pull_request_name": "Add search",
      "author_id": "u1",
      "status": "OPEN"
    },
    {
      "pull_request_id": "pr-1002",
      "pull_request_name": "Fix bug",
      "author_id": "u3",
      "status": "MERGED"
    }
  ]
}
```

Если пользователь нигде не ревьюер — валидный ответ с пустым массивом `pull_requests`.

---

### `POST /pullRequest/create`

Создание PR с автоназначением ревьюверов:

```bash
curl -X POST "http://localhost:8080/pullRequest/create" \
  -H "Content-Type: application/json" \
  -d '{
        "pull_request_id":   "pr-1001",
        "pull_request_name": "Add search endpoint",
        "author_id":         "u1"
      }'
```

Ответ `201`:

```json
{
  "pr": {
    "pull_request_id": "pr-1001",
    "pull_request_name": "Add search endpoint",
    "author_id": "u1",
    "status": "OPEN",
    "assigned_reviewers": ["u2", "u3"],
    "createdAt": "2025-11-16T17:40:00Z",
    "mergedAt": null
  }
}
```

Если автора/команды нет — `404`.
Если PR с таким ID уже существует — `409` с кодом `PR_EXISTS`.

---

### `POST /pullRequest/merge`

Идемпотентный merge PR:

```bash
curl -X POST "http://localhost:8080/pullRequest/merge" \
  -H "Content-Type: application/json" \
  -d '{
        "pull_request_id": "pr-1001"
      }'
```

Ответ `200`:

```json
{
  "pr": {
    "pull_request_id": "pr-1001",
    "pull_request_name": "Add search endpoint",
    "author_id": "u1",
    "status": "MERGED",
    "assigned_reviewers": ["u2", "u3"],
    "createdAt": "2025-11-16T17:40:00Z",
    "mergedAt": "2025-11-16T17:45:12Z"
  }
}
```

Повторный вызов с тем же `pull_request_id` — тоже `200`, тот же PR (идемпотентность).

---

### `POST /pullRequest/reassign`

Переназначить одного ревьюера на другого из его команды:

```bash
curl -X POST "http://localhost:8080/pullRequest/reassign" \
  -H "Content-Type: application/json" \
  -d '{
        "pull_request_id": "pr-1001",
        "old_user_id":     "u2"
      }'
```

Ответ `200`:

```json
{
  "pr": {
    "pull_request_id": "pr-1001",
    "pull_request_name": "Add search endpoint",
    "author_id": "u1",
    "status": "OPEN",
    "assigned_reviewers": ["u3", "u5"],
    "createdAt": "2025-11-16T17:40:00Z",
    "mergedAt": null
  },
  "replaced_by": "u5"
}
```

Варианты ошибок `409`:

* `PR_MERGED` — PR уже `MERGED`, менять нельзя.
* `NOT_ASSIGNED` — `old_user_id` не был ревьювером этого PR.
* `NO_CANDIDATE` — нет активного кандидата в команде заменяемого ревьювера.

---

### `GET /stats/assignments`

Дополнительный эндпоинт статистики: сколько раз кого назначали ревьювером.

```bash
curl "http://localhost:8080/stats/assignments"
```

Ответ `200`:

```json
{
  "assignments": [
    { "user_id": "u2", "count": 5 },
    { "user_id": "u3", "count": 3 }
  ]
}
```

---

### `GET /health`

Проверка живости сервиса:

```bash
curl "http://localhost:8080/health"
```

Ответ `200`:

```json
{ "status": "ok" }
```

---

#  Принятые допущения и обоснования

### 1. Отсутствие токенов / авторизации

В OpenAPI упоминаются `AdminToken` и `UserToken`, но **в задании они не описаны** — поэтому был выбран вариант **не реализовывать аутентификацию**, чтобы сосредоточиться на бизнес-логике (что соответствует формулировке "взаимодействие через HTTP").

### 2. Поведение при отсутствии ревьюверов

Если в команде автора нет активных участников (кроме самого автора), сервис корректно создаёт PR **без ревьюверов**, что соответствует ТЗ.

### 3. Поведение переназначения

Если нет доступных активных кандидатов — сервис возвращает доменную ошибку `NO_CANDIDATE` (как в ТЗ).

### 4. Массовая деактивация — не выполнено

Не реализовано из-за нехватки времени.
Если понадобится, в usecase уже есть база для этого: сервисы всех пользователей делают bulk-обновления.

---

# TODO / Что можно улучшить (если бы было больше времени)

* Добавить линтер (`golangci-lint`) и CI.
* Написать E2E тесты (`httptest.Server` + миграции в тестовом контейнере).
* Добавить PlantUML / Mermaid диаграммы.
* Реализовать массовую деактивацию команды + безопасную переназначаемость.

---

# Итог

Сервис:

* **полностью реализует OpenAPI спецификацию Авито**;
* выполняет *все обязательные требования задания*;
* поднимается командой `docker-compose up`;
* корректно мигрирует БД;
* соответствует ограничениям по производительности;
* включает одно дополнительное задание — **эндпоинт статистики** (+ нагружалка).

